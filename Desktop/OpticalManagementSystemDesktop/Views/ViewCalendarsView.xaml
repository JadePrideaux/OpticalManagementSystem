<UserControl x:Class="OpticalManagementSystemDesktop.Views.ViewCalendarsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OpticalManagementSystemDesktop.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="20">

        <!-- Back button -->
        <Button Content="Back to Main Menu"
                Command="{Binding BackToMenuCommand}"
                Margin="0,10,0,10"
                Width="200"/>

        <!-- Appointment slots -->
        <TextBlock Text="Appointments for Today:"
                   FontWeight="Bold"
                   Margin="0,0,0,10"/>
        <ListView ItemsSource="{Binding Slots}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Start Time" DisplayMemberBinding="{Binding StartTime}" />
                    <GridViewColumn Header="Patient ID" DisplayMemberBinding="{Binding Appointment.PatientId}" />
                    <GridViewColumn Header="Booked?" DisplayMemberBinding="{Binding IsBooked}" />
                </GridView>
            </ListView.View>
        </ListView>


    </StackPanel>
</UserControl>
